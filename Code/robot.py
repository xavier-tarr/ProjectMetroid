from SSC32 import SSC32
import numpy as np
import serial
import sys

class Robot:
    def __init__(self):
        lower= SSC32(self,9600,0)
        upper = SSC32(self,9600,1)
        current =0
        angle =0
        state =""

    def SINGLEROLL():
        while(signal)
        {
        if (state =='ball'):
            angle=checkangle()
            if (angle%60):
                lower.write([2000],current,"3")
                current +1
                if(current >5):
                    current =0
        else:
        raise ValueError("not in correct state")
                
                
                
            
        
            
        
